<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/gradientbackgroundpurple"
    tools:context=".AddingDrawFiles">

    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Follow the steps below to add custom colors, buttons, and drawable files, or click on the tutorial button for a short video."
        android:textColor="@color/smoke"
        android:textStyle="italic"
        android:textSize="18sp"
        android:padding = "10dp"/>

    <TextView
        android:id= "@+id/directionOnetextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/textView"
        android:text= "  Step 1: Click on res---> New ----> Drawable resource file "
        android:textAlignment="textStart"
        android:textStyle="bold"
        android:textSize="20sp"
        android:textColor="@color/smoke"
        android:padding = "10dp"/>


    <ImageView
        android:id="@+id/imageView2"
        android:layout_below="@id/directionOnetextView"
        android:layout_width="wrap_content"
        android:layout_height="200dp"
        app:srcCompat="@drawable/steponetutorial" />

    <Button
        android:id="@+id/tutorialButton"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:layout_below="@id/imageView2"
        android:layout_marginTop="8dp"
        android:layout_centerHorizontal="true"
        android:background="@drawable/white_rounded_buttom"
        android:onClick="buttonTutorialOnClick"
        android:text="Watch Short Tutorial"
        android:textColor="@color/purple"
        android:textStyle="bold" />

</RelativeLayout>